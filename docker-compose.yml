services:
  phenosuite:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3838:3838"
    volumes:
      - .:/srv/shiny-server/phenosuite
      - session_tmp:/apps/home/rtmp
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - RETICULATE_PYTHON=/opt/venv/bin/python
      - R_MAX_VSIZE=12Gb
      - PHENOSUITE_TMPDIR=/apps/home/rtmp
    shm_size: "2g"
    mem_limit: "13g"
    memswap_limit: "15g"

volumes:
  session_tmp:
